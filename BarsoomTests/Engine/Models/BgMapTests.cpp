#include "BgMapTests.hpp"

TEST(BgMapTest, ClipStartsEmpty) {
//    SDL_Renderer *renderer = SDL_CreateRenderer(window, -1, SDL_RENDERER_ACCELERATED);

//    ScreenManager screenManager(renderer);
    BgMap bgMap(0, 0, 0, 0);
}
